import React from "react";
import LoadingLayout from "@/core/layouts/LoadingLayout";

const loading = () => {
  return <LoadingLayout>กำลังโหลดหน้าอัพโหลดรายวิชา...</LoadingLayout>;
};

export default loading;
