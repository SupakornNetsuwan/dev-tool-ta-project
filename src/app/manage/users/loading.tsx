import React from "react";
import LoadingLayout from "@/core/layouts/LoadingLayout";

const loading = () => {
  return <LoadingLayout>กำลังโหลดหน้าจัดการผู้ใช้งาน</LoadingLayout>;
};

export default loading;
