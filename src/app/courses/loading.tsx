import React from "react";
import LoadingLayout from "@/core/layouts/LoadingLayout";

const loading = () => {
  return <LoadingLayout>กำลังโหลดหน้าจัดการคอร์ส</LoadingLayout>;
};

export default loading;
