import React from "react";
import LoadingLayout from "@/core/layouts/LoadingLayout";

const loading = () => {
  return <LoadingLayout>กำลังโหลดหน้าสมัครผู้ช่วยสอน</LoadingLayout>;
};

export default loading;
