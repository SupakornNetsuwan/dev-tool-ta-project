import React from "react";
import LoadingLayout from "@/core/layouts/LoadingLayout";

const loading = () => {
  return <LoadingLayout>กำลังโหลดหน้าจัดการข้อมูลส่วนตัว</LoadingLayout>;
};

export default loading;
